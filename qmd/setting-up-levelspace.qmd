# Setting Up LevelSpace {#sec-setting-up-levelspace}

```{r}
#| label: setup
#| include: false

library(here)

here("R", ".setup.R") |> source()
```

::: {.callout-important}
This manual is still under development and may be subject to change.

This warning will be removed once the manual is finalized.
:::

### Headless Configuration

The `setup` procedure comes with a `headless?` parameter to improve the model's performance when running in headless mode.

NetLogo already shut down some interface elements when in headless mode, such as monitors and plots, but it cannot disable internal model procedures. When `headless?` is set to `true`, the model skips all code related to `LogoClim` interface, which can significantly speed up simulations.

```netlogo
setup true
```
